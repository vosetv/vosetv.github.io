import React from 'react';
import PropTypes from 'prop-types';
import './styles.css';

const Filter = () => (
  <div className="filter">
    <div className="sort__text">Filter</div>
  </div>
);

export default Filter;
